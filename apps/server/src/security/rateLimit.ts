/**
 * rateLimit.ts
 * Stub and instructions to integrate rate limiting.
 * Recommended: use express-rate-limit in production.
 *
 * Example:
 *  const limiter = rateLimit({ windowMs: 10000, max: 30 });
 *  app.use("/api/", limiter);
 */

export const RATE_LIMIT_RECOMMENDATION = {
  windowMs: 10 * 1000,
  max: 30
};
